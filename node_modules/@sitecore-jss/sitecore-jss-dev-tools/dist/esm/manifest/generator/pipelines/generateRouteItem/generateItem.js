import { convertComponentDataToFields } from '../../utils';
export default (args) => {
    if (!args.route) {
        return args;
    }
    const item = Object.assign({}, args.route);
    delete item.children;
    delete item.path;
    delete item.placeholders;
    delete item.fields;
    item.layout = {
        renderings: [],
        placeholders: [],
    };
    if (args.route.fields) {
        item.fields = convertComponentDataToFields({ data: args.route.fields, context: { item } });
    }
    return Object.assign(Object.assign({}, args), { item });
};
