export default (args) => {
    const { dictionary } = args;
    return Object.assign(Object.assign({}, args), { pipelineResult: Object.assign(Object.assign({}, args.pipelineResult), { dictionary }) });
};
