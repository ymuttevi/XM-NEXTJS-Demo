"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const configLoader_1 = require("./configLoader");
const pipeline_1 = require("./pipeline");
const pipelineFactory_1 = require("./pipelineFactory");
const utils_1 = require("./utils");
const pipeline = pipelineFactory_1.pipelineFactory.create('Sample Pipeline');
pipeline.args.pipelineArg0 = 'pipelineArg0';
pipeline.addProcessor({
    name: 'proc0',
    modulePath: './test/pipelines/pipeline0/proc0.js',
    args: {
        fileArg0: 'fileArg0',
    },
});
pipeline
    .runWithCompilers(['babel-core/register'])
    .then((result) => {
    console.log(JSON.stringify(result, null, 2));
})
    .catch((err) => {
    console.error(err);
});
const runner = () => __awaiter(void 0, void 0, void 0, function* () {
    const compilers = ['babel-core/register'];
    return utils_1.initCompilers(compilers)
        .then(() => configLoader_1.configLoader({ fileGlobs: ['../test/pipelines/**/*.config.js'], workingDirectory: __dirname }))
        .then((config) => configLoader_1.configLoader({
        fileGlobs: ['../test/patch/**/*.config.js'],
        workingDirectory: __dirname,
        existingConfig: config,
    }))
        .then((config) => {
        const pipeline0 = config.pipeline0;
        pipeline0.args.arg0 = 'testArg';
        return pipeline_1.run(pipeline0);
    });
});
runner()
    .then((result) => {
    console.log(JSON.stringify(result, null, 2));
})
    .then(() => pipeline.run())
    .then((result) => {
    console.log(JSON.stringify(result, null, 2));
})
    .catch((err) => {
    console.error(err);
});
