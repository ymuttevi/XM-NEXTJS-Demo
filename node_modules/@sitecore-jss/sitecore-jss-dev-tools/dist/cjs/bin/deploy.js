#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const deploy_1 = require("../deploy");
const jss_config_1 = require("../setup/jss-config");
const options = {};
process.argv.forEach((value, index, map) => {
    switch (value) {
        case '--destinationPath':
            options.destinationPath = path_1.default.normalize(jss_config_1.replaceConfigTokens(map[index + 1]));
            break;
        case '--sourcePath':
            options.sourcePath = path_1.default.normalize(map[index + 1]);
            break;
        case '--excludeFile':
            options.excludeFile = map[index + 1];
            break;
        case '--clean':
            options.clean = true;
            break;
        default:
            break;
    }
});
if (!options.destinationPath) {
    console.error('deploy', 'please specify destination path via "--destinationPath" argument');
    process.exit(2);
}
if (!options.sourcePath) {
    console.error('deploy', 'please specify source path via "--sourcePath" argument');
    process.exit(2);
}
deploy_1.deploy(options);
