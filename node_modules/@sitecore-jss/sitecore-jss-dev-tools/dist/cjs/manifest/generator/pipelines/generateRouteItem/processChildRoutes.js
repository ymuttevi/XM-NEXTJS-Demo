"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const index_1 = require("./../../../../index");
exports.default = (args) => __awaiter(void 0, void 0, void 0, function* () {
    if (!args.route || !args.pipelines) {
        return args;
    }
    const { route, pipelines } = args;
    if (route.children && route.children.length > 0) {
        const pipeline = pipelines.generateRouteItem;
        pipeline.args = Object.assign(Object.assign({}, pipeline.args), args);
        const promises = route.children.map((childRoute) => __awaiter(void 0, void 0, void 0, function* () {
            pipeline.args.route = childRoute;
            return index_1.runPipeline(pipeline);
        }));
        const results = yield Promise.all(promises);
        return Object.assign(Object.assign({}, args), { item: Object.assign(Object.assign({}, args.item), { children: results.map((result) => result.item) }) });
    }
    return args;
});
