export interface PackageGenerateOptions {
    outputPath: string;
    appName: string;
    manifestPath: string;
    manifestFileName: string;
}
/**
 * @param {PackageGenerateOptions} options
 */
export declare function packageGenerate(options: PackageGenerateOptions): Promise<unknown>;
