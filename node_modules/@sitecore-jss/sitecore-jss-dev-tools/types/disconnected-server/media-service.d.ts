import { Request, Response } from 'express';
export interface DisconnectedAssetMiddlewareOptions {
    manifestPath: string;
    staticRootPath: string;
}
/**
 * @param {DisconnectedAssetMiddlewareOptions} config
 */
export declare function createDisconnectedAssetMiddleware({ manifestPath, staticRootPath, }: DisconnectedAssetMiddlewareOptions): (request: Request, response: Response) => void;
