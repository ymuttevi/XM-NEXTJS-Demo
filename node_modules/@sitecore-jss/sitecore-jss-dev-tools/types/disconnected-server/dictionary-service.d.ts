import { ManifestInstance } from '../index';
export interface DictionaryServiceOutput {
    lang: string;
    app: string;
    phrases: {
        [k: string]: string;
    };
}
export interface DisconnectedDictionaryServiceOptions {
    manifest: ManifestInstance;
    customizeDictionary?: (finalDictionary: DictionaryServiceOutput, rawDictionary: any, currentManifest: ManifestInstance, request?: any, response?: any) => DictionaryServiceOutput;
    manifestLanguageChangeCallback?: (language: string) => Promise<ManifestInstance>;
}
export declare function createDisconnectedDictionaryService({ manifest, customizeDictionary, manifestLanguageChangeCallback, }: DisconnectedDictionaryServiceOptions): {
    middleware: (request: any, response: any) => Promise<void>;
    updateManifest(newManifest: ManifestInstance): void;
};
