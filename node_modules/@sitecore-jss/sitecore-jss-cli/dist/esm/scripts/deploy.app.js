var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { builder as filesBuilder, handler as filesHandler } from './deploy.files';
import { builder as itemsBuilder, handler as itemsHandler } from './deploy.items';
export const command = 'app';
export const describe = 'Deploys files and items for the app to Sitecore. `jss deploy app --help` for options.';
export const builder = Object.assign(Object.assign({}, itemsBuilder), filesBuilder);
/**
 * @param {any} argv
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function handler(argv) {
    return __awaiter(this, void 0, void 0, function* () {
        return itemsHandler(argv)
            .then(() => filesHandler(argv))
            .catch((error) => {
            if (error) {
                console.log(error);
            }
            process.exit(1);
        });
    });
}
